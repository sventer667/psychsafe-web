// Retired: Connexus moved from a consultant/multi-client model to a direct
// self-service model where each Organization manages its own psychosocial
// risks. Industry/state/business unit now live on `organizations` (see
// routes/auth.ts, PATCH /api/auth/org). This file is unmounted in index.ts
// and kept only because files under the outputs mount can't be deleted.
export {}
