// Retired: replaced by Topbar.tsx (horizontal nav). Not imported anywhere.
// Kept as a harmless stub because files under the outputs mount can't be deleted.
export function Sidebar() {
  return null
}
