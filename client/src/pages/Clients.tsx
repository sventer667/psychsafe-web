// Retired: Connexus moved from a consultant/multi-client model to a direct
// self-service model where each Organization manages its own psychosocial
// risks. Organisation profile (industry/state/business unit) now lives on the
// Billing page. Not routed to in App.tsx; kept as a harmless stub because
// files under the outputs mount can't be deleted.
export function Clients() {
  return null
}
