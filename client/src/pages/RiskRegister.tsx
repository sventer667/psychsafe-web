// Retired: superseded by the per-case Hazard Register tab in CaseDetail.tsx (see /cases/:id).
// Kept as a harmless stub because files under the outputs mount cannot be deleted.
export function RiskRegister() {
  return null
}
