// Retired: superseded by the per-case Action Plan tab in CaseDetail.tsx (see /cases/:id).
// Kept as a harmless stub because files under the outputs mount cannot be deleted.
export function PreventionPlans() {
  return null
}
