// Retired: superseded by the per-case boardroom-ready PDF export (see Export tab in CaseDetail.tsx, /cases/:id).
// Kept as a harmless stub because files under the outputs mount cannot be deleted.
export function ManagementReport() {
  return null
}
