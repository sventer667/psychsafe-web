// Retired: superseded by action item status tracking in the per-case Action Plan tab (see /cases/:id).
// Kept as a harmless stub because files under the outputs mount cannot be deleted.
export function SignOffQueue() {
  return null
}
